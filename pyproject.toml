[project]
name = "epic"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "numpy",
    "pydantic-settings",
    "uvicorn[standard]",
]

[dependency-groups]
dev = [
    "httpx2",
    "pytest",
    "pytest-cov",
]
